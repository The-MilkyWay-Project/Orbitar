import { action } from '@storybook/addon-actions';

import Nav from 'kepler-4b';

export default {
  title: 'Navigation',
  component: Nav,
};

export const Navigation = () => ({
  Component: Nav,
});
